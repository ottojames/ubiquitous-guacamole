---
phase: 01-council-template-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/next/publish/config/placeholders.ts
  - src/pages/council/Templates.tsx
  - src/pages/council/TemplateValidationWarnings.tsx
autonomous: true

must_haves:
  truths:
    - "All 32 notice types have placeholder definitions"
    - "Required placeholder validation blocks template save"
    - "Validation errors display clearly before save attempt"
  artifacts:
    - path: "src/next/publish/config/placeholders.ts"
      provides: "Placeholder definitions for all notice types"
      contains: "PLACEHOLDER_REGISTRY"
    - path: "src/pages/council/TemplateValidationWarnings.tsx"
      provides: "Validation warning display"
      exports: ["default"]
  key_links:
    - from: "src/pages/council/Templates.tsx"
      to: "TemplateValidationWarnings"
      via: "component import and prop passing"
      pattern: "TemplateValidationWarnings"
---

<objective>
Audit placeholder coverage and enforce save-blocking on missing required placeholders.

Purpose: Ensure template validation is complete and enforced - templates cannot be saved without required placeholders, providing legal compliance guarantees for councils.

Output: All 32 notice types have placeholders defined; save button disabled when required placeholders missing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-council-template-system/01-RESEARCH.md
@src/next/publish/config/placeholders.ts
@src/next/publish/config/noticeTypes.ts
@src/pages/council/Templates.tsx
@src/pages/council/TemplateValidationWarnings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and complete placeholder coverage</name>
  <files>src/next/publish/config/placeholders.ts, src/next/publish/config/noticeTypes.ts</files>
  <action>
    1. Extract all notice type IDs from noticeTypes.ts (32 definitions)
    2. Check PLACEHOLDER_REGISTRY in placeholders.ts for each ID
    3. For any missing notice types, add placeholder definitions following the existing pattern:
       - Use COMMON_* spreads for shared placeholders
       - Add type-specific placeholders as needed
       - Mark required fields based on legislative requirements
    4. Ensure getPlaceholdersForNoticeType() returns non-empty array for all types

    Expected notice types to verify: licensing-premises-new, licensing-premises-variation, licensing-club-new, licensing-club-variation, gambling-betting-new/variation/review/transfer, gambling-bingo-new/variation/review/transfer, gambling-agc-new/variation/review/transfer, gambling-fec-new/variation/review/transfer, gvol-new, gvol-variation, planning-major, planning-eia, planning-listed, planning-conservation, planning-prow, planning-departure, probate-trustee-s27, tro-permanent, tro-temporary, tro-experimental
  </action>
  <verify>
    Run: `grep -c "noticeType:" src/next/publish/config/placeholders.ts` should return >= 32
    Create a quick test script that imports NOTICE_DEFINITIONS and getPlaceholdersForNoticeType, iterates all IDs, confirms none return empty array
  </verify>
  <done>All 32 notice types have placeholder definitions; no notice type returns empty placeholder array</done>
</task>

<task type="auto">
  <name>Task 2: Block save on missing required placeholders</name>
  <files>src/pages/council/Templates.tsx, src/pages/council/TemplateValidationWarnings.tsx</files>
  <action>
    1. In Templates.tsx, add validation state:
       - Import validateTemplatePlaceholders from placeholders.ts
       - Add state: `const [validationResult, setValidationResult] = useState<{isValid: boolean, missingRequired: string[]}>({isValid: true, missingRequired: []})`
       - Update validation on formData.template_text or formData.notice_type change

    2. Disable save button when validation fails:
       - In the save button, add `disabled={saving || !validationResult.isValid || !formData.template_text.trim()}`
       - Show tooltip/message explaining why save is disabled

    3. Update TemplateValidationWarnings.tsx:
       - Add prop `onValidationChange?: (isValid: boolean) => void`
       - Call onValidationChange when validation result changes
       - Make the "Missing Required Placeholders" section more prominent when blocking save

    4. Visual feedback:
       - Gray out save button when invalid
       - Add red border to editor when missing required placeholders
       - Clear messaging: "Add required placeholders to save"
  </action>
  <verify>
    1. Start dev server: `npm run dev`
    2. Navigate to council portal > Templates
    3. Click "Create Template"
    4. Select notice type, type text WITHOUT required placeholders
    5. Verify save button is disabled
    6. Add required placeholders via dropdown
    7. Verify save button enables when all required placeholders present
  </verify>
  <done>Save button disabled when required placeholders missing; clear visual feedback shown; button enables when all required present</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes
2. `npm run lint` passes (or only pre-existing warnings)
3. All 32 notice types return placeholders from getPlaceholdersForNoticeType()
4. Manual test: Cannot save template missing required placeholders
5. Manual test: Can save template with all required placeholders
</verification>

<success_criteria>
- Zero notice types return empty placeholder array
- Save button disabled when required placeholders missing
- Clear error messages shown for missing placeholders
- Save succeeds when all required placeholders present
</success_criteria>

<output>
After completion, create `.planning/phases/01-council-template-system/01-01-SUMMARY.md`
</output>
