---
phase: 01-council-template-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/council/TemplateTextEditor.tsx
  - src/pages/council/TemplatePreview.tsx
autonomous: true

must_haves:
  truths:
    - "Council staff sees live preview of rendered template as they type"
    - "Preview updates immediately when placeholders inserted"
    - "Preview shows example values for placeholders"
  artifacts:
    - path: "src/pages/council/TemplateTextEditor.tsx"
      provides: "Side-by-side editor with preview panel"
      min_lines: 500
    - path: "src/pages/council/TemplatePreview.tsx"
      provides: "Placeholder substitution rendering"
      exports: ["default"]
  key_links:
    - from: "src/pages/council/TemplateTextEditor.tsx"
      to: "TemplatePreview"
      via: "component import"
      pattern: "import.*TemplatePreview"
    - from: "src/pages/council/TemplatePreview.tsx"
      to: "placeholders.ts"
      via: "getPlaceholdersForNoticeType"
      pattern: "getPlaceholdersForNoticeType"
---

<objective>
Add side-by-side live preview to template editor showing rendered output with example values.

Purpose: CONTEXT.md specifies "Side-by-side live preview - editor on left, rendered preview on right, updates as you type". This gives council staff confidence in what their final notice will look like.

Output: Template editor shows editor and preview panels; preview substitutes placeholders with example values; updates in real-time.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-council-template-system/01-CONTEXT.md
@.planning/phases/01-council-template-system/01-RESEARCH.md
@src/pages/council/TemplateTextEditor.tsx
@src/next/publish/config/placeholders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TemplatePreview component</name>
  <files>src/pages/council/TemplatePreview.tsx</files>
  <action>
    Create new component `TemplatePreview.tsx` that:

    1. Props interface:
       ```typescript
       interface TemplatePreviewProps {
         templateText: string;
         noticeType: string;
         className?: string;
       }
       ```

    2. Substitution logic:
       - Import getPlaceholdersForNoticeType from placeholders.ts
       - Get placeholders for the notice type
       - Build a map of token -> example value from placeholder definitions
       - Replace all `{{TOKEN}}` patterns with their example values
       - Highlight substituted values with a subtle background (e.g., bg-blue-50)

    3. Rendering:
       - Parse the templateText (handle HTML from contentEditable)
       - Apply placeholder substitutions
       - Render with appropriate styling (match final notice appearance)
       - Show "Preview" header with info icon explaining example values are shown

    4. Handle edge cases:
       - Empty template: Show placeholder message "Start typing to see preview"
       - Unknown placeholder tokens: Show them as-is with warning color
       - Missing example: Use token name as fallback
  </action>
  <verify>
    `npm run typecheck` passes with new component
    Component can be imported: `import TemplatePreview from './TemplatePreview'`
  </verify>
  <done>TemplatePreview component exists; accepts templateText and noticeType props; renders substituted preview</done>
</task>

<task type="auto">
  <name>Task 2: Integrate preview into TemplateTextEditor</name>
  <files>src/pages/council/TemplateTextEditor.tsx</files>
  <action>
    Modify TemplateTextEditor.tsx to show side-by-side layout:

    1. Layout restructure:
       - Wrap existing editor in a flex container
       - Left side (50%): Existing editor (toolbar, contentEditable, footer)
       - Right side (50%): TemplatePreview component
       - Add divider between panels (visual separator)
       - Make layout responsive: stack vertically on mobile (lg:flex-row)

    2. Wire up preview:
       - Import TemplatePreview component
       - Pass value (templateText) to preview
       - Pass noticeType to preview
       - Preview updates automatically as value changes (React re-render)

    3. Styling considerations:
       - Match editor height to preview height (min-h-[500px] on both)
       - Add subtle shadow/border to preview panel
       - Preview panel header: "Live Preview" with info tooltip
       - Keep existing editor styling unchanged

    4. Toggle option (optional enhancement):
       - Add "Show Preview" toggle for users who prefer full-width editor
       - Store preference in localStorage
       - Default: preview shown
  </action>
  <verify>
    1. Start dev server: `npm run dev`
    2. Navigate to council portal > Templates > Create Template
    3. Verify editor and preview are side by side
    4. Type in editor - preview updates
    5. Insert placeholder - preview shows example value
    6. Resize window - layout stacks on mobile
  </verify>
  <done>TemplateTextEditor shows side-by-side editor and preview; preview updates in real-time; responsive layout works</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes
2. `npm run lint` passes (or only pre-existing warnings)
3. Manual test: Side-by-side layout visible in template editor
4. Manual test: Preview shows placeholder substitutions with example values
5. Manual test: Preview updates as user types
6. Manual test: Responsive - stacks on mobile widths
</verification>

<success_criteria>
- Side-by-side layout: editor left, preview right
- Preview substitutes {{TOKEN}} with example values
- Real-time updates as user types
- Mobile-responsive layout
- Clear visual distinction between editor and preview panels
</success_criteria>

<output>
After completion, create `.planning/phases/01-council-template-system/01-02-SUMMARY.md`
</output>
